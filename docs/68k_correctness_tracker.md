# 68000 Correctness Tracker

Last updated: 2026-06-19

This is the detailed done/missing tracker for CPU-visible 68000 behavior. Keep
`docs/progress.md` as the narrative log, and update this file whenever a TDD
slice changes what is covered, partial, or missing.

## Status Key

- **Done**: implemented and covered by a passing unit/generated-exec test.
- **Partial**: some decode/emission/runtime behavior exists, but the entry is not
  complete enough to trust as correct 68000 behavior.
- **Missing**: required for a correct 68000/NeoGeo implementation and not yet
  covered by the current tests.
- **Unknown**: needs a real-ROM smoke, oracle/fuzz run, or spec-driven audit
  before it can be classified.

Current verification command:

```sh
ctest --test-dir build --output-on-failure
```

Last local verification: **10/10 passing** on 2026-06-19.

## Done and Covered

| Area | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Behavior-based generated-C harness | Done | `tests/test_generated_exec.c` builds emitted C into the test and compares it with a small interpreter oracle. | This is the strongest current correctness check, but it only covers the synthetic fixture. |
| Static emission sweep for recognized opcodes | Done | `tests/test_opcode_sweep.c`; current `ctest` passes. | Scope is limited to opcodes the decoder already recognizes as non-`UNKNOWN`. It does not prove every valid 68000 encoding is decoded. |
| Decode truncation rejection | Done | `tests/test_m68k_decode.c`; decoder uses `finish_decode()` in `recompiler/src/m68k_decode.c`. | Decoded instructions whose declared byte length extends outside the ROM are rejected. |
| `NOP` no-op behavior | Done | `tests/exec_fixture.h`, `tests/test_generated_exec.c`; `recompiler/src/c_emitter.c`. | Generated-exec now has a standalone `NOP` fixture proving `$4E71` falls through to the following instruction without changing data/address registers, CCR/SR, or memory before the final `STOP`; the interpreter oracle now models `$4E71` directly. |
| PC-relative `LEA` base | Done | `tests/test_m68k_decode.c`, `tests/test_generated_exec.c`; implementation in `recompiler/src/m68k_decode.c`. | The displacement base is the extension-word address, not the following instruction address. Decode now records the long-sized control source and address-register destination for the PC-displacement form as well as the target. |
| Immediate `MOVE` to data registers | Done | `tests/test_generated_exec.c`, `tests/test_c_emitter.c`; implementation in `recompiler/src/c_emitter.c`. | `MOVE.B/W/L #imm,Dn` generated paths write the correct destination width and update long-sized flags for `MOVE.L` while preserving `X`. |
| `MOVEA.W` sign extension | Done | `tests/test_generated_exec.c`; `emit_movea_generic()` in `recompiler/src/c_emitter.c`. | Word-sized sources are sign-extended to 32-bit address registers and condition codes are not affected. |
| `MOVEA.W` PC-index sign extension | Done | `tests/test_m68k_decode.c`; `tests/exec_fixture.h`; `tests/test_generated_exec.c`; `recompiler/src/m68k_decode.c`; `recompiler/src/c_emitter.c`. | Decode/format coverage now distinguishes `MOVEA.W` from `MOVEA.L` PC-index forms and records source/destination EA metadata. Generated-exec covers `MOVEA.W (d8,PC,Dn.W),An`, verifying the extension-word PC-relative base, word-index address calculation, word source sign-extension into the address register, and CCR preservation. |
| Address-register arithmetic word sign extension | Done | `tests/test_generated_exec.c`; `emit_alu_address_reg()` in `recompiler/src/c_emitter.c`. | `ADDA.W` and `SUBA.W` sign-extend word sources to 32 bits and leave condition codes unchanged; `CMPA.W` sign-extends the source, leaves `X` unchanged, and sets `N/Z/V/C` from the 32-bit subtraction result. |
| `CMPI.B` data-register overflow flags | Done | `tests/exec_fixture.h`; `tests/test_generated_exec.c`; `recompiler/src/c_emitter.c`. | Generated-exec now covers byte immediate compare against a data register where `$80 - $01` sets `V`, clears `N/Z/C`, preserves `X`, and leaves the destination data register unchanged. |
| `CMPM` postincrement stack-pointer byte step | Done | `tests/test_generated_exec.c`; `emit_cmpm()` in `recompiler/src/c_emitter.c`. | `CMPM.B (A7)+,(Ax)+` now has generated-exec coverage for the MC68000 byte-sized stack-pointer postincrement-by-two rule, memory comparison flags, and `X` preservation. |
| `CMPM` opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `CMPM` opcode size, source postincrement register, destination postincrement register, 2-byte length, and no-stray-field metadata to match decoded metadata. |
| `NBCD` predecrement stack-pointer byte step | Done | `tests/test_generated_exec.c`; `emit_nbcd_generic()` in `recompiler/src/c_emitter.c`. | `NBCD -(A7)` now has generated-exec coverage for the MC68000 byte-sized stack-pointer predecrement-by-two rule, memory writeback, sticky `Z`, and decimal borrow into `X/C`. |
| Extend/BCD pair opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `ADDX`/`SUBX` and byte-only `ABCD`/`SBCD` register/predecrement forms to have opcode mnemonic, size, R/M form, source register, and destination register bits matching decoded metadata. |
| `TAS` data-register test-before-set | Done | `tests/test_generated_exec.c`; `emit_tas_generic()` in `recompiler/src/c_emitter.c`. | `TAS Dn` now has generated-exec coverage proving flags are computed from the original byte, bit 7 is set afterward, `V/C` clear, and `X` is preserved. |
| `EXG` register exchange forms | Done | `tests/test_generated_exec.c`; `emit_exchange()` in `recompiler/src/c_emitter.c`. | Data-register, address-register, and data-register/address-register pairs are covered as 32-bit swaps with condition codes unaffected. |
| `PEA` PC-relative effective-address push | Done | `tests/test_generated_exec.c`; `emit_ea_address_value()`/`NG_M68K_PEA` in `recompiler/src/c_emitter.c`. | `PEA (d16,PC)` now has generated-exec coverage proving the pushed long uses the extension-word PC-relative base and condition codes are unaffected. |
| `LEA` PC-index effective-address load | Done | `tests/test_generated_exec.c`; `emit_ea_address_value()`/`NG_M68K_LEA` in `recompiler/src/c_emitter.c`. | `LEA (d8,PC,Dn.W),An` now has generated-exec coverage proving extension-word PC-relative base plus word-index address calculation and condition codes unaffected. |
| `DIVU.W` quotient overflow | Done | `tests/test_generated_exec.c`; `emit_divide()` in `recompiler/src/c_emitter.c`. | Unsigned 32-by-16 division whose quotient exceeds 16 bits now has generated-exec coverage proving `V` set, `C` clear, `X` preserved, and destination data register unchanged. |
| `DIVU.W`/`DIVS.W` successful quotient/remainder packing | Done | `tests/test_generated_exec.c`; `emit_divide()` in `recompiler/src/c_emitter.c`. | Generated-exec covers successful unsigned zero-quotient, unsigned bit-15 quotient, signed negative quotient/remainder, and signed zero-quotient cases. The fixture verifies upper-word remainder/lower-word quotient packing, `X` preservation, `C/V` clearing, and `N/Z` derived from the quotient. |
| `MULU.W`/`MULS.W` word result and flags | Done | `tests/test_generated_exec.c`; `emit_multiply()` in `recompiler/src/c_emitter.c`. | Generated-exec covers unsigned high-bit products, signed negative products, and signed zero products. The fixture verifies the low word of the destination data register is multiplied with the word source into a full 32-bit result, with `X` preserved, `C/V` cleared, and `N/Z` derived from the 32-bit product. |
| `CHK`/multiply/divide opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires word-sized `CHK`, `MULU`/`MULS`, and `DIVU`/`DIVS` opcodes to match the decoded mnemonic, destination data register, source EA bits, extension lengths, and immediate width. |
| `MOVEM` control-mode ordering and sign-extension slice | Done | `tests/test_generated_exec.c`; `emit_movem()` in `recompiler/src/c_emitter.c`. | Generated-exec covers register-to-memory long transfers through address-displacement and absolute-word control destinations, memory-to-register long transfers through address-displacement sources, memory-to-register word transfers through absolute-long and PC-relative sources, D/A mask ordering, word sign-extension into both data and address registers, and CCR preservation. |
| `MOVEM` indexed sparse-mask slice | Done | `tests/test_generated_exec.c`; `emit_movem()` in `recompiler/src/c_emitter.c`. | Generated-exec covers address-indexed control-mode long transfers with sparse D/A masks in both directions, including word-index sign extension, D0/D3/A1/A6 register-to-memory order, D1/D4/A2/A5 memory-to-register order, CCR preservation, and computing the indexed effective address before loading a listed base address register. The fixture generator now uses the bounded discovery helper with a larger candidate budget so the expanded generated-exec address set does not overflow discovery storage. |
| `MOVEM` PC-index source slice | Done | `tests/test_generated_exec.c`; `emit_movem()` in `recompiler/src/c_emitter.c`. | Generated-exec covers a word-sized memory-to-register transfer through `(d8,PC,Dn.W)`, verifying the extension-word PC base, word-index sign extension, sparse D/A mask order, word sign-extension into data and address registers, and CCR preservation. |
| `MOVEM` opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `MOVEM` opcode direction, size bit, transfer EA bits, register-mask width, instruction length, and direction-specific EA metadata to match decoded metadata. |
| Shift/rotate nonzero edge flags | Done | `tests/test_generated_exec.c`; `emit_shift_rotate_dreg()` and `emit_shift_rotate_memory()` in `recompiler/src/c_emitter.c`. | Generated-exec covers nonzero `ASL.B`, `ASR.W`, `LSR.L`, `ROXR.B`, and `ROR.W` data-register cases plus memory `ROXL.W` and `ASL.W`, verifying result bits, `X/C` last-shifted-bit behavior, pure-rotate `X` preservation, zero results, arithmetic-left overflow, and memory word writes. |
| Shift/rotate opcode/decode legality slice | Done | `tests/test_m68k_decode.c`, `tests/test_m68k_validate.c`; `recompiler/src/m68k_decode.c`, `recompiler/src/m68k_validate.c`. | Memory shift/rotate decode now rejects non-canonical bit-11 encodings such as `$E8D0`, and validator coverage requires register and memory shift/rotate opcode direction, kind, size, count source, and EA bits to match decoded metadata. |
| `MOVEP` alternate-byte transfers | Done | `tests/test_m68k_decode.c`, `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; `emit_movep()` in `recompiler/src/c_emitter.c`. | Word/long decode and static emission are covered; generated-exec now covers a long `Dn`-to-memory-to-`Dn` alternate-byte round trip and confirms condition codes are not affected. |
| `MOVEP` EA payload validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires both `MOVEP` directions to use canonical no-stray data-register and address-displacement EA payloads in addition to exact opcode, size, register, displacement, and length metadata. |
| `JSR`/`BSR` return push | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; `emit_push_return_address()` in `recompiler/src/c_emitter.c`. | Generated code pushes the following PC as a 32-bit return address. |
| `JSR`/`BSR` tail-dispatch | Done | `tests/test_c_emitter.c`; `recompiler/src/c_emitter.c`. | Generated code dispatches to the subroutine and returns from the host function instead of continuing host-side immediately. |
| Post-`JSR`/`BSR` continuation discovery | Done | `tests/test_function_discovery.c`; `recompiler/src/function_discovery.c`. | Continuation addresses are added so `RTS` can return through the generated dispatch table. |
| `RTS` stack return dispatch | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; `recompiler/src/c_emitter.c`. | `RTS` reads the return PC from `(A7)`, increments `A7` by 4, and dispatches to that PC. Generated-exec covers a stack-manipulating return pattern (`ADDQ.L #4,A7; RTS`) so a callee can skip its local return address and return to the caller's caller. |
| Direct and control-EA `JMP`/`JSR` emission | Done | `tests/test_c_emitter.c`, `tests/test_m68k_decode.c`. | Static discovery is still only conservative for direct/control targets it can prove. |
| Conditional/unconditional branch target discovery | Done | `tests/test_function_discovery.c`; `recompiler/src/function_discovery.c`. | Discovery now seeds taken `Bcc` targets in addition to fall-through continuations and treats `BRA` as a tail branch, seeding the taken target without scanning unreachable fall-through bytes as code. `DBcc` target candidates are seeded except for never-branching `DBT`. |
| Exception-return CFG termination | Done | `tests/test_function_discovery.c`; `recompiler/src/function_discovery.c`. | `RTE` and `RTR` now terminate static candidate scanning because control resumes from the stacked PC/CCR frame rather than from the following ROM word; unreachable fall-through bytes are no longer seeded as instruction starts. |
| Invalid/unvalidated CFG boundary | Done | `tests/test_function_discovery.c`; `recompiler/src/function_discovery.c`. | Static discovery now consults the post-decode validator before seeding fall-through instruction starts. `UNKNOWN`, `INVALID`, zero-length, or otherwise unvalidated decoded words terminate speculative scanning instead of turning data/frontier bytes into additional generated function candidates. |
| `RTE`/`RTR` basic stack pop and dispatch | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; `recompiler/src/c_emitter.c`. | `RTE` is supervisor-guarded and restores SR through the generated SR helper, so returning to user mode switches from `SSP` back to `USP`. Generated-exec also covers unprivileged `RTR` popping CCR-only condition codes from the active stack, preserving the supervisor/system byte, popping the PC, and dispatching to the restored target. Broader interrupt recognition remains separate partial work. |
| Basic exception vector stack path | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; `ng_push_exception_frame()` emitted by `recompiler/src/c_emitter.c`. | Covered for emitted trap-style paths, canonical `ILLEGAL`, A-line/F-line emulator exceptions, taken `TRAPV`, failed `CHK`, and divide-by-zero, including user-to-supervisor stack selection before pushing SR/PC frames. Generated-exec now oracle-checks `ILLEGAL` vector 4, A-line vector 10, F-line vector 11, `TRAPV` vector 7 preserving the saved SR and next PC, `CHK` vector 6 for both negative values (`N` set in saved SR) and above-bound values (`N` cleared in saved SR), plus `DIVU.W` divide-by-zero vector 5 preserving the destination register and saving the pre-trap SR/next-PC frame. Exact exception priorities and bus/address-error frames remain partial. |
| `STOP` decode/emission hook | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; runtime hook `ng_m68k_stop_until_interrupt()`. | Immediate SR install, supervisor enforcement, and the runtime stop hook are covered. Basic interrupt wake from `STOP` is tracked separately below. |
| `RESET` external-device hook | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; `ng_m68k_reset_devices()` in `include/ngrecomp/neogeo_runtime.h` and `runtime/src/neogeo_runtime.c`. | Supervisor-mode `RESET` now calls an explicit runtime reset hook while leaving the 68000 state/condition codes unchanged; user-mode `RESET` still vectors through privilege violation before the hook can run. The default runtime hook is currently a no-op until the NeoGeo secondary-device reset model is implemented. |
| `MOVE USP` transfer emission | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; state fields in `include/ngrecomp/m68k_state.h`. | Transfer instructions are emitted and guarded by supervisor-mode privilege checks. |
| Supervisor/user stack switching for emitted code | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; generated `ng_set_sr()` helper in `recompiler/src/c_emitter.c`. | Full-SR writes, `STOP`, exception entry, and `RTE` now switch active `A7` between `USP` and `SSP` when the S bit changes. |
| Privilege violations for emitted privileged instructions | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; generated `ng_require_supervisor()` helper in `recompiler/src/c_emitter.c`. | User-mode `STOP`, `RESET`, `RTE`, `MOVE <ea>,SR`, `MOVE USP`, and `ORI/ANDI/EORI #imm,SR` now vector through privilege-violation vector 8 with the faulting instruction PC. MC68000 `MOVE SR,<ea>` remains unprivileged; later-family `MOVE from SR` privilege is out of current MC68000 scope. |
| Basic interrupt entry from `STOP` | Done | `tests/test_c_emitter.c`, `tests/test_function_discovery.c`, `tests/test_generated_exec.c`; generated `ng_service_interrupt()` helper and runtime `ng_m68k_take_interrupt()` hook. | `STOP` now installs the immediate SR, lets the runtime arm an interrupt, accepts runtime-approved interrupts according to the current mask, stacks SR/PC on `SSP`, sets supervisor mode plus the interrupt mask, vectors through the supplied vector number, and returns through `RTE` to the post-`STOP` continuation. A masked interrupt fixture verifies no wake/frame when the level is not allowed. |
| Runtime-supplied instruction-boundary interrupts | Done | `tests/test_c_emitter.c`, `tests/test_function_discovery.c`, `tests/test_generated_exec.c`; per-instruction `ng_service_interrupt()` checks in generated C. | Generated code now polls for runtime-approved interrupts before each emitted instruction. Accepted interrupts stack the current instruction PC, vector to the handler, and `RTE` returns through discovered instruction-start continuations. |
| Runtime interrupt mask and level-7 controller | Done | `tests/test_runtime_interrupts.c`; runtime `ng_m68k_set_interrupt_level()`, `ng_m68k_clear_interrupt_level()`, and `ng_m68k_take_interrupt()`. | The default runtime now tracks an asserted IPL level/vector, inhibits levels lower than or equal to the current SR mask, and treats a lower-to-level-7 transition as a one-shot nonmaskable edge even when the current mask is 7. |
| NeoGeo cartridge interrupt source model | Done | `tests/test_runtime_interrupts.c`; runtime `ng_neogeo_request_*_interrupt()` and `ng_neogeo_ack_interrupts()`. | VBlank, timer, and reset-pending sources map to cartridge-system autovector levels 1, 2, and 3 respectively, with IRQACK-style bits selecting which pending sources to clear. Highest pending level drives the runtime IPL controller. |
| Memory-mapped `REG_IRQACK` | Done | `tests/test_runtime_interrupts.c`; runtime `ng68k_write8()`/`ng68k_write16()` handling for `$3C000C`. | Word writes to `$3C000C` and low-byte writes to `$3C000D` clear the corresponding pending NeoGeo IRQ sources through `ng_neogeo_ack_interrupts()`. |
| LSPC timer register model and frame/VBlank scheduling | Done | `tests/test_runtime_interrupts.c`; runtime `REG_LSPCMODE`, `REG_TIMERHIGH`, `REG_TIMERLOW`, `REG_TIMERSTOP`, `ng_neogeo_begin_vblank()`, `ng_neogeo_advance_timer()`, `ng_neogeo_advance_scanline()`, and `ng_neogeo_advance_frame()`. | Covered behavior includes timer reload-value writes, reload-on-`REG_TIMERLOW`, reload-on-VBlank, reload-on-zero repeat mode, interrupt-enable gating, VBlank IRQ requests on explicit VBlank/frame start and NTSC scanline wrap, timer IRQ priority over pending VBlank, IRQACK clearing, odd-byte GPU/LSPC register writes duplicating the byte into both halves, and NTSC 384-pixel/264-scanline frame advancement. This is not yet tied to generated CPU cycle accounting. |
| Runtime P-ROM, system-ROM, work-RAM, palette-RAM, and backup-RAM bus slices | Done | `tests/test_runtime_interrupts.c`; `ng_neogeo_set_program_rom()`, `ng_neogeo_set_system_rom()`, `ng68k_read*()`, and `ng68k_write*()` in `runtime/src/neogeo_runtime.c`. | The default runtime can back fixed P-ROM reads, the repo's banked P-ROM window, cartridge-system BIOS/system ROM reads and mirrors at `$C00000-$CFFFFF`, 64KiB work RAM reads/writes, banked/mirrored palette RAM at `$400000-$7FFFFF`, palette bank selects at `REG_PALBANK1/0`, palette byte-write duplication into both bytes of a word, MVS backup RAM at `$D00000-$DFFFFF`, `REG_SRAMLOCK/UNLOCK` write gating, big-endian word/long helpers, and 24-bit address wrapping for covered regions. P-ROM/system-ROM writes are ignored/read-only. Broader NeoGeo device regions remain partial/missing. |
| Basic generated architectural PC maintenance | Done | `tests/test_generated_exec.c`; `recompiler/src/c_emitter.c`. | Generated code now writes `g_ng_m68k.pc` at generated dispatch entry, before each emitted instruction's interrupt poll, after normal fall-through, and after `STOP` completion. Generated-exec covers interrupt-resumed straight-line code, trace-handler `STOP`, and illegal-handler `STOP`. |
| Codegen diagnostics for unsupported/decode failures | Done | `tests/test_c_emitter.c`; `NgEmitDiagnostics` and `ng_emit_c_checked()` in `recompiler/src/c_emitter.c`. | Checked C emission now records unsupported decoded instructions and decode errors and fails generation instead of silently relying only on generated runtime dispatch misses. |
| Validator fallback rejection | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Post-decode validation now rejects out-of-range or otherwise unhandled mnemonic enum values instead of silently accepting them through the switch fallback. |
| Immediate op opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `ORI`/`ANDI`/`SUBI`/`ADDI`/`EORI`/`CMPI` opcode class, size bits, destination EA bits, immediate width, extension length, and legacy metadata to match decoded metadata. |
| Binary op opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires binary `ADD`/`SUB`/`CMP`/`OR`/`AND`/`EOR` opcode operation, direction, size, source register, destination register, source/destination EA bits, extension lengths, and legacy metadata to match decoded metadata. |
| `TST` opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `TST` opcode size and source EA bits, data-alterable source legality, extension length, and legacy metadata to match decoded metadata. |
| Bit-op opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires static and dynamic `BTST`/`BCHG`/`BCLR`/`BSET` opcode kind, dynamic source data-register bits, destination EA bits, extension lengths, and legacy metadata to match decoded metadata. |
| Address-register op opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `MOVEA`, `ADDA`, `SUBA`, and `CMPA` opcode family, size/opmode bits, destination address-register bits, source EA bits, extension length, and legacy metadata to match decoded metadata. |
| `MOVE` opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires generic `MOVE` opcode size, source EA bits, destination EA mode/register bits, extension lengths, and legacy metadata to match decoded metadata. |
| Control-source opcode validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `JMP`/`JSR`/`PEA`/`LEA` opcode family, source EA bits, and `LEA` destination address-register bits to match decoded metadata. |
| `JMP`/`JSR` metadata validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `JMP`/`JSR` form, target, PC-displacement, and no-stray absolute/displacement metadata to match decoded control-source metadata. |
| `PEA` metadata validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `PEA` top-level target, absolute-address, and displacement metadata to stay empty while the exact control-source EA payload carries the address. |
| `LEA` metadata validation slice | Done | `tests/test_m68k_validate.c`; `recompiler/src/m68k_validate.c`. | Validator coverage now requires `LEA` PC-displacement targets/displacements, absolute and PC-index targets, and no-stray target/displacement/form metadata for non-targeting control sources to match decoded metadata. |
| Post-decode legality validator | Done | `tests/test_m68k_validate.c`, `tests/test_m68k_decode.c`; `recompiler/src/m68k_validate.c`, `recompiler/src/m68k_decode.c`. | The validator rejects `UNKNOWN`/`INVALID`, out-of-range/unhandled mnemonic enum values, illegal control-EA uses, illegal `MOVE`/`MOVEA` destinations and missing sources, exact MC68000 `MOVE` opcode/source/destination EA payloads, extension lengths, legacy metadata, and byte-sized address-register source rejection while preserving word/long `MOVE An,<ea>` legality, invalid condition numbers, selected data-alterable requirements, `ADDQ`/`SUBQ` quick-count/address-register-size/no-source, exact opcode/immediate/size/destination EA payloads/lengths, and no-stray-field metadata, exact `TST` opcode/data-alterable operand payloads, instruction lengths, and legacy metadata plus `CMPI` non-alterable destinations and malformed metadata, exact immediate logical/arithmetic/compare opcode/data-alterable destination payloads, immediate widths, instruction lengths, and legacy metadata, invalid binary `ADD`/`SUB`/`CMP`/`OR`/`AND`/`EOR` source-destination shapes, exact opcode/source/destination EA payloads, PC-relative bases, index-register metadata, data-register payloads, canonical legacy form/top-level fields, instruction lengths, and source-register metadata, word/long-only `ADD`/`SUB`/`CMP` address-register sources, memory-alterable `ADD`/`SUB`/`OR`/`AND` destinations, data-alterable `EOR` destinations, exact word/long `MOVEA`/`ADDA`/`SUBA`/`CMPA` opcode/source EA payloads, instruction lengths, PC-relative bases, immediate widths, address-register destinations, and no-stray legacy metadata, exact word-sized `CHK`, `MULU`/`MULS`, and `DIVU`/`DIVS` opcode/source EA payloads, PC-relative bases, immediate widths, destination-register consistency, instruction lengths, and no-stray legacy fields, exact `BTST` static/dynamic opcode/source metadata, read-only data-addressing destination EA payloads including PC-relative and dynamic-immediate destinations while rejecting static-immediate `BTST #n,#data`, exact altering bit-op opcode/source/immediate/length metadata, data-alterable destination EA payloads, legacy fields, and no-stray-field metadata for `BCHG`/`BCLR`/`BSET`, exact `EXG` opcode/opmode register-exchange metadata, exact `LINK`/`UNLK` opcode/register stack-frame metadata, exact `MOVEP` opcode/size/direction canonical address-displacement/data-register metadata, exact `MOVEM` opcode/register-list transfer metadata, direction-specific EA payloads, MOVEM extension-base PC-relative payloads, and lengths, `CMPM` exact opcode/postincrement EA payloads, 2-byte metadata, register-field consistency, and no-stray-field metadata, exact `ADDX`/`SUBX` opcode/register/predecrement pair payloads and byte-only `ABCD`/`SBCD` opcode/register/predecrement pair payloads with no-stray-field metadata, exact shift/rotate opcode/register/memory EA payloads, Dn count/destination metadata, word-only one-bit memory forms, instruction lengths, and no-stray-field metadata, exact `Scc` opcode/condition data-alterable destination EA payloads and metadata, and exact `DBcc` opcode/condition counter/displacement metadata, including byte-sized no-source `Scc` data-alterable destination metadata and word-sized 4-byte operand-free `DBcc` target/displacement metadata, invalid `ORI`/`ANDI`/`EORI` to `CCR`/`SR` sizes/immediates plus exact opcode/no-operand metadata, exact MC68000 `MOVE SR,<ea>`/`MOVE <ea>,SR`/`MOVE <ea>,CCR` opcode/source/destination EA payloads, immediate widths, PC-relative bases, lengths, and no-stray-field metadata with 68010+ `MOVE CCR,<ea>` rejected, exact `MOVE USP` opcode/direction metadata, long-sized address-register payloads, empty opposite operand, and no-stray-field metadata, exact MC68000 `BRA`/`BSR`/`Bcc` byte/word branch displacement, target, opcode/condition, length, and no-stray-field metadata, including `$FF` as a valid byte displacement of `-1` rather than a 68020 long extension, exact destination-only unary `CLR`/`NEG`/`NEGX`/`NOT` opcode/destination EA payloads, legacy form metadata, and no-stray-field metadata, exact byte-only `NBCD`/`TAS` opcode/destination EA payloads, legacy form metadata, and no-stray-field metadata, exact `MOVEQ` opcode/register sign-extended-immediate/data-register metadata, exact `EXT`/`SWAP` opcode/register-only metadata, malformed `LEA` metadata, exact control-source `JMP`/`JSR`/`PEA`/`LEA` opcode/source EA payloads, `JMP`/`JSR` form/target/displacement/no-stray metadata, `PEA` no-stray top-level address metadata, `LEA` destination register bits, PC-relative bases, index displacement ranges, top-level targets/displacements, lengths, and metadata, and exact fixed-opcode/no-operand metadata for `NOP`, `RESET`, `RTE`, `RTR`, `RTS`, and `TRAPV`, exact opcode-to-vector metadata for `ILLEGAL`/A-line/F-line, exact opcode/immediate metadata for `STOP` and `TRAP`, and invalid/stray-field system/control metadata for `ILLEGAL`/A-line/F-line, `STOP`, and `TRAP`. `EXG`, `MOVEQ`, `DBcc`, and PC-displacement `LEA` decode now mark architectural size/source/destination metadata, and byte `MOVE An,<ea>` decodes to `UNKNOWN` so opcode sweeps do not emit invalid MC68000 forms. Checked emission consults the validator before emitting an instruction. |
| Game TOML discovery metadata parsing | Done | `tests/test_game_config.c`, `tests/test_function_discovery.c`; `recompiler/src/game_config.c`, `recompiler/src/function_discovery.c`, and `recompiler/src/main.c`. | `--game` now parses `[functions].entry`, `[functions].extra`, `[game].discovery_files`, and manual `[[jump_table]]` metadata. Discovery-file paths are resolved relative to their parent config, and abs32/pcrel16/BRA jump-table targets are merged into the same seed set with the cartridge entry while unmapped seeds are ignored. |
| Dispatch/control-flow audit enforcement hook | Done | `tests/test_dispatch_audit.c`; `recompiler/src/dispatch_audit.c`, `recompiler/src/main.c`. | The audit classifies discovered dispatch sites as direct, computed runtime targets, or PC-index jump tables; reports missing direct targets and missing/resolved jump-table entries; can be emitted from the CLI with `--emit-dispatch-audit <out.txt>`; and `--fail-on-dispatch-gaps` makes smoke runs exit nonzero when direct, computed, table-missing, or truncated gaps remain. |
| Basic trace exception entry | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; generated `ng_service_trace()` helper. | Generated code snapshots the SR T bit before executing each instruction. Linear fall-through instructions, taken `BRA`/`Bcc`, taken `DBcc`, `JSR`/`BSR`/`JMP`, `RTS`, `STOP`, `RTE`/`RTR`, `TRAP`, taken `TRAPV`, failed `CHK`, and divide-by-zero paths now vector through trace vector 9 after execution when trace was enabled at instruction start. Trace stacks the architectural next/target PC, or for traced instruction exceptions the exception-handler PC after that exception frame is built, plus current saved SR on `SSP`, and clears the live T bit during exception entry. Generated-exec also covers no-trace behavior for not-executed illegal, A-line, F-line, and privilege cases, plus trace-before-pending-interrupt priority. |
| All 16 condition predicates matrix-covered for `Scc` | Done | `tests/test_c_emitter.c`, `tests/test_generated_exec.c`; condition predicate helper in `recompiler/src/c_emitter.c`. | Generated-exec now evaluates all 16 `Scc` predicates across all 16 `N/Z/V/C` combinations and verifies condition stores plus CCR preservation. Branch forms share the helper but still only have selected control-flow fixtures. |
| All 16 condition predicates matrix-covered for `DBcc` | Done | `tests/test_generated_exec.c`; condition predicate helper and label-target filtering in `recompiler/src/c_emitter.c`. | Generated-exec now evaluates all 16 `DBcc` predicates across all 16 `N/Z/V/C` combinations on both counter-exhaustion fall-through and taken-branch paths. Coverage verifies true predicates leave `D0` unchanged, false predicates decrement the low word, false-with-nonnegative-counter branches skip a flag-neutral marker, false-to-`$FFFF` falls through, and CCR is preserved; codegen suppresses impossible local labels for never-branching `DBT`/`BF` target candidates. |
| `Bcc` condition control-flow matrix | Done | `tests/test_generated_exec.c`; condition predicate helper and branch emission in `recompiler/src/c_emitter.c`. | Generated-exec now evaluates all 14 architectural `Bcc` condition encodings (2-15; 0/1 are `BRA`/`BSR`) across all 16 `N/Z/V/C` combinations, verifying taken branches skip a flag-neutral marker and CCR is preserved. |

## Partial / Needs Broader Proof

| Area | Status | Current behavior | Missing before it is trusted |
| --- | --- | --- | --- |
| Opcode coverage | Partial | Current sweep proves every decoder-recognized non-`UNKNOWN` opcode emits without an unsupported stub, and checked emission fails on unsupported decoded instructions. | A spec-driven legality matrix and/or trusted-disassembler audit for every valid 68000 opcode/addressing form. |
| Effective-address legality | Partial | Generic EA helpers, many decode/emission paths, and a growing post-decode validator exist. Recent validator coverage includes immediate logical/arithmetic/compare opcode/data-alterable destinations plus immediate-width/instruction-length checks and no-source EA payload rejection, quick-op exact opcode/immediate/size/destination payload/no-source/instruction-length/no-stray-field checks, exact binary `ADD`/`SUB`/`CMP`/`OR`/`AND`/`EOR` opcode/source/destination EA payloads, PC-relative bases, index-register metadata, data-register payloads, canonical legacy form/top-level fields, instruction lengths, and source-register metadata, word/long-only `ADD`/`SUB`/`CMP` address-register sources, memory-alterable `ADD`/`SUB`/`OR`/`AND` destinations, data-alterable `EOR` destinations, exact `MOVEA`/`ADDA`/`SUBA`/`CMPA` opcode/source EA payloads, word/long source lengths, PC-relative bases, immediate widths, address-register destination consistency, and no-stray legacy metadata, exact `MOVE` opcode/source/destination EA payloads, byte/word/long extension lengths, legacy-form consistency, address-register source size restrictions, exact word `CHK`/multiply/divide opcode/source EA payloads, PC-relative bases, immediate widths, destination-register consistency, lengths, and no-stray legacy metadata, exact `TST` opcode/data-alterable operand payloads/legacy fields with implicit destination empty-payload rejection plus exact `CMPI` immediate and data-alterable destination metadata, exact `BTST` static/dynamic opcode/source metadata and read-only data-addressing destination payloads versus altering bit-op opcode/source/immediate/length, data-alterable destination payloads, legacy fields, and no-stray-field metadata, exact `MOVEM` opcode direction/size/EA bits, direction-specific EA classes and payloads, implicit register-list-side empty-EA payload rejection, MOVEM extension-base PC-relative payloads, register-mask width, instruction lengths, and no-stray-field metadata, exact `MOVEP` opcode/size/direction canonical address-displacement/data-register pairing, instruction length, register-field consistency, displacement consistency, and no-stray-field metadata, `CMPM` postincrement-only forms with exact opcode/source/destination EA payloads, 2-byte metadata, register-field consistency, and no-stray-field metadata, exact `ADDX`/`SUBX` opcode/register/predecrement pair payloads and byte-only `ABCD`/`SBCD` opcode/register/predecrement pair payloads with no-stray-field metadata, exact `EXG` opcode/register-only opmode, length, register-field, and no-stray-field metadata, exact destination-only unary `CLR`/`NEG`/`NEGX`/`NOT` and byte-only `NBCD`/`TAS` destination EA payloads, legacy form metadata, and no-stray-field metadata, exact `MOVEQ` opcode/register sign-extended-immediate/data-register metadata, exact `EXT`/`SWAP` opcode/register-only length, size, form, register-field, and no-stray-field metadata, exact `LINK`/`UNLK` stack-frame length, register, size, displacement, no-EA, and no-stray-field metadata, exact `Scc` opcode/condition data-alterable destination EA payloads, extension-length, no-source, and no-stray-field metadata, exact `DBcc` opcode/condition register-counter, displacement-target, no-EA, and no-stray-field metadata, exact control-source `JMP`/`JSR`/`PEA`/`LEA` opcode/source EA payloads, `LEA` destination register bits, PC-relative bases, index displacement ranges, instruction lengths, unsized `JMP`/`JSR` form/target/displacement/no-stray metadata including empty destination payloads, long-sized `PEA` no-stray metadata including empty destination payloads and `LEA` metadata, static `JMP`/`JSR` form/target/displacement/no-stray metadata consistency, `LEA` address-register destination plus target/displacement consistency, exact shift/rotate opcode/register/memory EA payloads, immediate/register-count metadata, word-only one-bit memory forms, instruction lengths, and no-stray-field metadata, `MOVE USP` exact address-register-only transfers with no-stray-field metadata, exact `MOVE SR`/`MOVE CCR` source/destination EA payloads, implicit SR/CCR-side no-EA payload rejection, immediate widths, PC-relative bases, lengths, and MC68000-only direction filtering, immediate `CCR`/`SR` operation sizes and no-stray no-operand metadata including empty-EA payload fields, and exact MC68000 byte/word-only branch target/displacement encodings. | Expand the validator to every remaining instruction family's legal source/destination EA combinations and verify all valid EA forms per family. |
| Condition codes | Partial | `N`/`Z` and selected `C`/`X`/`V` paths are implemented where tests cover them, including all 16 `Scc` and `DBcc` condition predicates plus all 14 real `Bcc` condition encodings across every `N/Z/V/C` combination, with `DBcc` now covered for both counter-exhaustion fall-through and taken-branch paths. Other covered flag slices include byte `ADD` overflow/carry, byte `SUB` borrow, byte `CMP` overflow with `X` preserved, `CMPI.B #imm,Dn` overflow with `X` preserved, `CMPM.B` memory-compare `N/V/C` with `X` preserved, failed `CHK.W` setting/clearing `N` in the saved exception SR for negative/above-bound operands, `TAS` data-register flags from the original byte with `X` preserved, address-register `ADDQ`/`SUBQ` and `ADDA`/`SUBA` CCR preservation, `CMPA.W` preserving `X` while updating `N/Z/V/C`, successful `DIVU.W`/`DIVS.W` quotient `N/Z` plus `C/V/X` behavior, `MULU.W`/`MULS.W` product `N/Z` plus `C/V/X` behavior, register-count-zero and selected nonzero `ASL`/`ASR`/`LSR`/`ROR`/`ROXL`/`ROXR` shift/rotate behavior, `ABCD`/`SBCD` sticky-`Z`/`X`/`C` behavior for register and predecrement-memory cases, and `NBCD` data-register plus predecrement-`A7` sticky-`Z`/borrow behavior. | Oracle/fuzz coverage for exact `X/N/Z/V/C` behavior across remaining arithmetic sizes/EAs, logical, shifts/rotates, remaining BCD/NBCD EA cases, compare, extend, and divide edge cases. |
| Arithmetic/divide semantics | Partial | `MULS/MULU`, `DIVS/DIVU`, `ADD/SUB/CMP`, extend arithmetic, BCD, and unary RMW paths exist. Word-form `MULU/MULS` fixtures cover unsigned high-bit products, signed negative products, zero products, full 32-bit destination writes, and `N/Z/V/C/X` behavior. `DIVS/DIVU` divide-by-zero now enters the shared exception-frame path and honors trace-after-instruction-trap priority; generated-exec oracle coverage now proves `DIVU.W #0,Dn` vectors through vector 5, preserves the destination data register, and saves the pre-trap SR plus next PC before the handler runs. Signed `DIVS` and unsigned `DIVU` quotient-overflow fixtures cover unaffected destinations plus `V`/`C`/`X` behavior; successful `DIVU.W`/`DIVS.W` fixtures cover zero quotient, bit-15 quotient, negative signed quotient/remainder, result packing, and `N/Z/V/C/X` behavior. Generated-exec also covers selected byte `ADD`/`SUB`/`CMP`/`CMPI` flag edge cases, byte `CMPM` postincrement-memory comparison with `A7` byte-step behavior, address-register `ADDQ`/`SUBQ` full-32-bit no-CCR behavior, and `ADDA.W`/`SUBA.W`/`CMPA.W` word-source sign-extension with the required CCR effects; BCD fixtures cover register and predecrement-memory `ABCD`/`SBCD` decimal carry/borrow behavior plus `NBCD` data-register and predecrement-`A7` sticky-zero/borrow behavior. Divide-by-zero condition-code results remain architecturally undefined, so the checked invariant is the exception frame/state parity, not newly defined post-trap flags. | Exact undefined/unaffected flags, remaining `NBCD`/BCD EA cases, and broader arithmetic behavior against a trusted 68000 oracle. |
| `MOVEM` | Partial | Long/word register-list transfers and predecrement paths are emitted for covered cases. The validator now enforces exact opcode/register-list transfer metadata: direction bit, word/long size bit, transfer EA bits, 16-bit mask width, exact instruction lengths, no stray legacy fields, exact register-to-memory control-alterable/predecrement destination payloads, exact memory-to-register control/postincrement source payloads, and MOVEM-specific PC-relative bases after the mask word. Generated-exec covers normal-mask long transfers, address-displacement, address-indexed, and absolute-word register-to-memory control destinations, address-displacement, address-indexed, absolute-long, PC-displacement, and PC-indexed memory-to-register control sources, sparse D/A mask order in both indexed directions, extension-word PC bases, word-index sign extension for indexed effective addresses, word memory-to-register sign-extension into data and address registers, postincrement self-load behavior where the addressing register receives the final EA, plus MC68000 predecrement mask reversal and initial-addressing-register storage for `MOVEM.L #$0001,-(A7)`. | Full ordering/mask behavior across remaining valid EA/size combinations, especially additional sparse word and predecrement combinations, still needs oracle fixtures. |
| Exception stack semantics | Partial | Basic SR/PC exception frames, supervisor stack selection, and vector dispatch are emitted for covered generated paths, including oracle-backed `ILLEGAL` vector 4, `TRAPV` vector 7, A-line/F-line vectors 10/11, `CHK` vector 6, and `DIVU.W` divide-by-zero vector 5. Generated-exec now also checks an `ILLEGAL` handler-visible SR path where exception entry clears the live `T` bit while the stacked saved SR preserves the pre-exception `T`. | Exact vector fetch ordering, address/bus error frames, and the full exception-priority matrix. |
| Interrupt model | Partial | `STOP` and ordinary instruction boundaries can accept runtime-approved interrupts, stack a format-0 frame, update the interrupt mask, vector to a handler, and return via `RTE`. The default runtime has IPL mask, level-7-edge tracking, cartridge VBlank/timer/reset-pending source APIs, memory-mapped `REG_IRQACK` clearing, and a frame/scanline-advanced LSPC timer/VBlank scheduler. | Interrupt priority against other exceptions, generated CPU-cycle integration, PAL `REG_TIMERSTOP` behavior, and real host pacing. |
| Trace mode | Partial | Generated code uses the T bit from instruction start and services trace exceptions after linear fall-through, taken branches/`DBcc`, subroutine calls, jumps, `RTS`, `STOP`, `RTE`/`RTR`, `TRAP`, taken `TRAPV`, failed `CHK`, and divide-by-zero, before the next instruction-boundary interrupt poll. Exception entry clears the live T bit while preserving saved SR, not-executed illegal/A-line/F-line/privilege cases do not trace, pending interrupts are taken after trace, and the oracle now shares the same live-`T` clearing rule for covered exception entries. | Broader spec/oracle coverage across every remaining exception class and generated path, especially address/bus-error paths once implemented. |
| Function discovery / CFG | Partial | Direct calls, validated instruction-start continuations, `JSR`/`BSR` continuations, `STOP` continuations, conditional `Bcc` taken targets, `DBcc` taken targets except `DBT`, tail `BRA`/`JMP` targets, terminal `RTS`/`RTE`/`RTR` boundaries, one PC-index jump-table shape, game-config function seeds, discovery-file seed merging, manual abs32/pcrel16/BRA jump-table metadata, validator-gated invalid/frontier boundaries, and dispatch/control-flow audit enforcement are implemented. | Indirect jump tables beyond the current/manual patterns, data-driven targets, stronger code-vs-data boundary classification after other exception/trap paths, and robust real-ROM CFG recovery without excessive duplicate generation. |
| `g_ng_m68k.pc` | Partial | Generated code now maintains `PC` at dispatch entry, instruction-boundary interrupt polls, linear fall-through, and `STOP` completion, with generated-exec coverage for interrupt, trace-handler, and illegal-handler paths. | Broader proof that every branch/subroutine/`RTE`/exception path and future address/bus-error path leaves architectural `PC` correct at each observable boundary. |
| Real-ROM frontier tracking | Partial | `docs/progress.md` records the last Metal Slug frontier and notes it is stale. | Rerun the Metal Slug smoke and replace stale `DC.W` frontier data with the current first failure. |
| Runtime bus boundary | Partial | Generated code uses `ng68k_read*`/`ng68k_write*` and runtime hooks. The default runtime now covers fixed/banked P-ROM reads, cartridge-system BIOS/system ROM reads, 64KiB work RAM, banked/mirrored palette RAM, MVS backup RAM with lock/unlock writes, selected system/LSPC/IRQACK registers, and 24-bit wrapping for covered regions. | Correct remaining NeoGeo memory map/device behavior: watchdog, inputs, sound latch/Z80 communication, BIOS/system registers beyond current latch bits, VRAM-facing ports, protection/bank-switch variants, persistent save backing, and unmapped/bus-error policy. |

## Missing for Correct 68000 Behavior

| Area | Status | Required work |
| --- | --- | --- |
| CPU-cycle-tied LSPC timer / VBlank scheduling | Missing | Connect the covered frame/scanline timer model to generated CPU cycle accounting or host pacing, model PAL `REG_TIMERSTOP` timing, and verify exception-priority rules against CPU exceptions. |
| Full exception/trace priority matrix | Missing | Broaden priority coverage across every generated exception class, address/bus errors once implemented, and nested exception/vector-fetch failure cases. |
| Address/bus error precision | Missing | Handle odd word/long accesses and unmapped/bus-error cases with correct exception behavior if required by real software. |
| Full oracle/fuzz harness | Missing | Add a trusted 68000 reference runner or generated randomized fixtures for every implemented instruction family, size, EA mode, and edge case. |
| Cycle timing / prefetch | Missing | No cycle timing or prefetch behavior is modeled. Only add this once functional correctness and real-ROM needs demand it. |
| NeoGeo hardware integration | Missing | Implement/test watchdog, inputs, sound latch/Z80 communication, BIOS/system registers beyond current latch bits, VRAM-facing ports, protection/bank-switch variants, persistent save backing, VBlank/level integration with host pacing, and remaining unmapped/bus-error behavior. |
| Real game boot checkpoint | Missing | Metal Slug has not yet reached a documented boot/attract-mode checkpoint in this tracker. |

## Cross-Repo Reference Notes

A local contrast against `/Users/dudu/Projects/references/segagenesisrecomp` lives in
[`segagenesisrecomp_contrast.md`](segagenesisrecomp_contrast.md). Main actionable
lessons: add centralized codegen diagnostics, a post-decode legality validator,
game-TOML-driven discovery metadata, dispatch/jump-table audits, and a trusted
68000 oracle harness. The reference's Genesis-specific `A7 = SSP` simplification
should **not** replace the real supervisor/user stack switching tracked here.

## Ordered TDD Backlog

1. **Broaden post-decode legality validator**: keep making invalid
   source/destination effective-address combinations and MC68000-family scope
   explicit for every decoded instruction family.
2. **Trusted oracle/fuzz harness**: compare generated C against an external or
   embedded 68000 reference for per-instruction semantic edge cases.
3. **Condition-code and arithmetic edge fixtures**: broaden generated-exec/oracle
   coverage for exact `X/N/Z/V/C` behavior, divide overflow, extend arithmetic,
   BCD, compare, and shift/rotate edge cases.
4. **Full exception/trace priority matrix**: broaden coverage across every
   generated exception class, address/bus errors once implemented, and nested
   exception/vector-fetch failure cases.
5. **Dispatch and metadata audit enforcement**: expand `games/*.toml` beyond
   function/discovery-file/jump-table metadata and broaden dispatch-audit target
   patterns beyond the current direct/computed/PC-index cases.
6. **Real-ROM smoke refresh**: regenerate Metal Slug C, capture the current first
   frontier, add a regression, and update this tracker.
7. **CPU-cycle-tied LSPC timer / VBlank integration**: connect the covered
   frame/scanline timer model to generated CPU cycle accounting or host pacing
   and add priority tests against CPU exceptions.
8. **NeoGeo bus slices**: continue implementing one hardware-visible region
   at a time with a failing runtime/generated-exec test first; palette RAM,
   basic MVS backup RAM, and cartridge-system BIOS/system ROM reads are now
   covered, next candidates are inputs, sound latch/Z80 communication,
   watchdog, and VRAM-facing ports.

## Update Rules

- Move an item to **Done** only with a named passing test or generated-exec
  oracle fixture.
- Keep **Partial** when code exists but coverage is narrow or architectural
  behavior is not complete.
- Add the test name, source file, and behavioral invariant whenever a row moves.
- After each green slice, update both this tracker and `docs/progress.md` before
  committing.
